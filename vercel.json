{
  "version": 2,
  "framework": "vite",
  "root": "client",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "github": {
    "silent": true,
    "autoAlias": true
  }
} 